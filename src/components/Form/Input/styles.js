import { StyleSheet } from 'react-native';

import { COLOR_SCHEME } from '../../../constants';

export default StyleSheet.create({
    textInput: {
        color: COLOR_SCHEME.ACTION,
        borderBottomWidth: 2,
        borderBottomColor: COLOR_SCHEME.ACTION,
        marginTop: 20,
        paddingVertical: 10
    }
});
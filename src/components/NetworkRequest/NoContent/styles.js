import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems: 'center'
    },
    
    message: {
        marginTop: 20,
        marginBottom: 50,
        color: '#fff'
    }
});
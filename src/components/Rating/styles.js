import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    starView: {
    },

    ratingList: {
        flex: 1,
        flexDirection: 'row'
    },

    star: {
        flexDirection: 'row',
        marginRight: 5
    }
});